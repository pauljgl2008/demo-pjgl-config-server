server:
  port: 8085

spring:
  application:
    name: configserver
  profiles:
    active: git
  cloud.config.server.git:
    uri: https://github.com/pauljgl2008/demo-pjgl-enviroments-secrets
    clone-on-start: true
    default-label: main
